apiVersion: v2
name: alloy
description: >
  Grafana Alloy — library chart providing shared named templates (deployment,
  service, configmap, helpers). Sourced as a dependency by alloy-otlp,
  alloy-prom, and alloy-discovery. Not deployable on its own.
type: library
version: 0.1.0
appVersion: "1.7.0"
