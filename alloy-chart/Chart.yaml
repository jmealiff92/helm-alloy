apiVersion: v2
name: alloy
description: >
  Grafana Alloy — deployable standalone or consumed as a Helm dependency by
  alloy-otlp, alloy-prom, and alloy-discovery. Child charts override values
  under the alloy: key; this chart's templates always render the resources.
type: application
version: 0.1.0
appVersion: "1.7.0"
